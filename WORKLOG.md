# PT Worklog

Running log of changes. Newest at top. Keep entries to 1–2 sentences.

> **TODO (eval):** plots use `last10_eval_reward` (mean of last 10 in-training IQL evals, to match the noise/flip plots). When time allows, run a **separate 50-episode evaluation** of the final policies and plot *that* instead — applies to all PT sweeps (noise, flip, blind-oracle).

> **USER LABELING / DEMO TO-DO** (what's left for the human to do — when asked "what's left to label/demo", answer from here):
> 1. **[DO FIRST] B=5 blanking redo (N=350 PT / ~50 GAIL, 10 seeds):**
>    - PT — ✅ **p50 DONE** (350/350, 0 skips, 128.7 min → `humanblock5p50-s0..s9`). ⬜ **p75 remains**: label `pt_human/videos_human_n350_block5_p75` → `humanblock5p75`. (0% = reuse `humanblock0`, now extended to `-s0..s9`.)
>    - GAIL — play `DIFFICULTY=blank PCT={50,75} BLOCK_LEN=5 bash collect_human_demos_lunarlander.sh` (IRL3/imitation; **the only launcher** — it pins `--fps 20`, derives `blank5_p{PCT}`, and writes `session_meta.json`). Flag 50 with `F`; press `Q` to finish. → `frame_blanking/demos/human/blank5_p{50,75}/session_1_flagged`. (0% = reuse `human_demos/lunarlander/session_2`, done — **but note it was played at `--fps 25` = 0.45× real time**, vs 20 = 0.40× for these; see the speed-audit row.)
> 2. **[READY NOW] Lander vanishing (N=100 PT / N=15 GAIL, 10 seeds, b=5, fps 20, levels 0/25/50/75):**
>    - PT — label `lander_vanish/pt_human/videos_human_n100_vanish{25,50,75}` (100 pairs each, ~35–40 min per level) via `label_web.py` (use `--resume`) → convert to `humanvanish{25,50,75}`, copy to `-s0..s9`. **0% is already done** — `make_vanish0_labels.py` derived it from the first 100 clean labels, so only three levels need labelling.
>    - GAIL — play four sessions, flagging exactly **15** each: `DIFFICULTY=vanish PCT={25,50,75} BLOCK_LEN=5 FPS=20 FLAG_TARGET=15 bash collect_human_demos_lunarlander.sh`, plus the **clean baseline as its own fps-20 collection** (`DIFFICULTY=none FPS=20 FLAG_TARGET=15 OUTPUT_DIR=$SCRATCH/imitation_runs/lander_vanish/demos/human/vanish_p0`) — do **not** reuse `session_2`, it was played at fps 25 and would put a speed difference on the baseline.
>    - Then Claude: `PCT={0,25,50,75} sbatch scripts/lander_vanish/run_pt_pipeline_vanish.sh` (40 jobs) + `bash IRL3/scripts/lander_vanish/submit_gail_vanish_human.sh <pct> 0 9` (40 jobs) → `scripts/plots/plot_lander_vanish_human.py`.
> 3. **[NEXT] Action delay (control axis; PT N=100 / GAIL N=15, 10 seeds; PT k=0/5/10/20, GAIL k=0/5/10):**
>    - GAIL — play `DIFFICULTY=delay K={5,10} FPS=20 FLAG_TARGET=15 FEEDBACK=both bash collect_human_demos_lunarlander.sh` → `action_delay/demos/human/delay_k{K}`. **k=0 reuses the existing `vanish_p0` clean fps-20 session** (= delay 0). ⚠️ k=10 = 500 ms felt lag, hard.
>    - PT — Claude generates fresh **delayed 4-source mixtures** (drop medium-replay) from the PT SAC policies for k=0/5/10/20, extracts 100 pairs each; user labels all four via `label_web.py --resume` → `humandelay{k}`. **No free k=0 here** (fresh build).
>    - Then Claude: train PT (dataset = the delayed mixture per k) + GAIL, 10 seeds → `plot_lander_delay_human.py`.
> 4. **[FUTURE] Region masking Sweep 1 (N=350 PT / ~50 GAIL, 10 seeds):**
>    - PT — label `region_mask/pt_human/videos_human_n350_region{25,50,75}` → `humanregion{25,50,75}`. (0% = reuse `humanblock0`.)
>    - GAIL — play `DIFFICULTY=region PCT={25,50,75} bash collect_human_demos_lunarlander.sh` → `region_mask/demos/human/region{25,50,75}/session_1_flagged`, flag 50 each. (0% = reuse `session_2`.)
> After each level exists, Claude does: convert → train PT+GAIL 10 seeds → plot.
>
> **SESSION SNAPSHOT (2026-07-08)** — durable checkpoint (context is being compacted).
> - **Part A — DONE + plotted.** PT N=350 coin random-replace noise (330/330 `grid_mixture_ms/lunarlander-grid-ms-N350-{clean,noise*}`) + GAIL N=50 coin noise (300/300, `gail_grid_noise_N50_2026-07-07.csv`). Plot `scripts/plots/plot_noise_N350_N50.py` → `figures/noise_N350_N50_pt_vs_gail.png`. Result: PT 262→**−52** @100% (robustness returns at N=350); GAIL 263→**−204**. Nothing left to do here.
> - **Part B — WAITING ON USER (manual labeling/playing).** Human frame-blanking redo at **B=5 (0.25 s), levels 0/50/75, 10 seeds, coin**, one plot (PT **blue** + GAIL **green**). PT videos rendered: `frame_blanking/pt_human/videos_human_n350_block5_p{50,75}` (350 pairs, 50.7%/75.1% blanked, index local) → user labels via `label_web` (ports 8711/8712). GAIL: user plays `human_demo.py --blank-mode block --block-len 5 --blank-prob {0.5,0.75}`, flag ~50 each → `blank5_p50/p75`. **0% reuses** `humanblock0` (PT) / `session_2` (GAIL). Then I: convert labels → train PT+GAIL 10 seeds → build `frame_blanking_human_b5` plot.
> - **Region masking — Sweep 1 (size axis), HUMAN ARMS, building.** Fixed black rectangle per unit; size sweep 0/25/50/75% (area, √f box), location random-per-unit seeded (shared across the 10 training seeds), N=350 PT / ~50 GAIL, same rendered segments as the B=5 blanking arm. PT videos generating (`region_mask/pt_human/videos_human_n350_region{25,50,75}`, job 5758426). GAIL: `human_demo.py --region-frac {0.25,0.5,0.75}` ready. **0% reuses** `humanblock0` (PT) / clean session (GAIL). Agent arms (blind-oracle/expert) + Sweep 2 (location as controlled variable) deferred. Next (after user labels/plays): convert → train PT+GAIL 10 seeds → region plot.

| Date | What | Why / Result |
|---|---|---|
| 2026-07-27 | **ACTION DELAY — new control-difficulty axis, design locked (PT + GAIL, human arms).** Each action is applied `k` sim steps late (FIFO primed with no-ops; `make_control_corruptor`). **k = steps × 20 ms game time**; at fps 20 the human *feels* k × 50 ms. **PT levels k = 0/5/10/20; GAIL k = 0/5/10** (PT gets the extra k=20 to probe whether reward-learning still ranks near-uniformly-bad flights — expert land rate 100/88/22/~0% at k=0/5/10/20). **Key asymmetry (accepted):** GAIL has a live control loop so the human feels the lag → degraded demos; PT has none, so delay is baked into *trajectory generation* — the compared clips show worse flying but the labeller watches a perfect view at normal speed (a data-distribution change, not a perceptual difficulty). **PT uses its OWN SAC policies** (`/scratch/marzii/PT/lunarlander/seed_0/sac_run/{medium,expert}/actor.zip`, `LunarLanderContinuous-v3`, delay queue primed with the ZERO VECTOR) — **NOT the GAIL expert 4615187**. **Fuller-mixture recipe:** re-roll medium+expert under delay (random reused, delay-invariant), **drop medium-replay from all levels** (it's a training buffer, can't be delayed) → fresh 4-source mixture per k; so **PT k=0 is a fresh build with its own ~100 labels, not free**. All new artifacts under a new `action_delay/` slug; nothing existing is mutated (only flag-gated `--delay-k` / `--no-medium-replay` additions, default off). | First control-interface axis. Sticky was rejected (p=0.5 changes only ~19% of expert actions). Design in `~/project_docs/DIFFICULTY_AXES_OVERVIEW.md` + plan `mellow-bubbling-parasol.md`. |
| 2026-07-23 | **Lander vanishing: the fill now RECONSTRUCTS the background instead of guessing a colour** (user's idea). Erased pixels used to be filled from the nearest surviving pixel in the same column, snapped to pure black or pure white. **User asked what happens when the lander is over a flag** — and the answer is that it breaks badly: `(204,204,0)` has brightness 408 against a 382 threshold, so a covered flag snaps to **WHITE**. Measured: **662 frames** affected, worst cases erasing **131/343 flag px (38%)** and leaving a bright white block where the lander was — a louder marker than the lander itself (`figures/lander_vanish_flag_fix.png`). Fix: `build_plate()` takes the per-pixel **median over ~48 frames spread across the FULL EPISODE** (mapped via `extract_segment_videos.find_episode`, cached per episode), which is the scene with no lander in it; erased pixels are pasted from that. **The full episode is required, not the 100-frame clip**: on `pair_007` the lander had already landed before the clip starts, so 1,152 px are covered in all 100 segment frames — over the 911-frame episode, 0. Median rather than a fixed handful of frames because a sample can catch the lander parked at a pixel (a 6-frame sample still failed on 1,265 px there). Verified on the worst flag frame: flag px 343 original → **212 old fill / 349 new**; plate covers **100.00%** of pixels, 0 fallback. `inpaint_columns` is kept only as the fallback for pixels the plate never saw. | Two of the seven leaks (ghost streaks, flag notches) both came from *inferring* the fill colour. Copying the measured background has no such failure mode — nothing is inferred, so there is no rule left to get wrong. |
| 2026-07-23 | **LANDER VANISHING — new perception axis, built (human arms, N=100 PT / N=15 GAIL, levels 0/25/50/75, b=5, fps 20).** Replaces whole-frame blanking: during a blanked block the **lander alone** is removed and the hole is filled from the terrain behind it, so ground/pad/flags stay fully visible but the craft is untrackable. PT side: `scripts/preference/blank_lander_videos.py` (hull colour-match + largest-component, plus the WHOLE engine plume — particles are colour-**tinted** (spread ~119) while terrain anti-aliasing is pure grey (spread 0), which is what separates plume from horizon; column inpaint **snapped** to pure black/white to kill ghost streaks). Built via `scripts/preference/build_vanish_query_sets.sh` (sbatch) → `lander_vanish/pt_human/videos_human_n100_vanish{25,50,75}`, each a copy of **pair_000–099 of the clean 350** so the 0% point is comparable. **0% costs no human time**: `scripts/preference/make_vanish0_labels.py` subsets the first 100 clean labels → `humanvanish0-s0..s9` (49 A / 41 B / 10 tie), and *verifies* those 100 line up entry-for-entry with the vanish sets' `metadata.pkl` before writing. New `scripts/lander_vanish/run_pt_pipeline_vanish.sh` (`PCT=0\|25\|50\|75 sbatch …`, array 0-9) and `scripts/plots/plot_lander_vanish_human.py` (auto-skips untrained levels). GAIL side lives in IRL3 (`imitation/lander_vanish.py`, `--difficulty vanish`). | Whole-frame blanking removes *everything*, so it confounds "can't see the lander" with "can't see anything". Vanishing isolates the craft. Three information leaks were found by **eyeballing, not by numbers**: a vertical ghost streak from copying contaminated neighbours; a persistent particle trail; and — worst — particles left visible when the lander was off-screen, because the mask early-returned on no-hull. All three fixed. |
| 2026-07-23 | **Low-budget pairing: PT N=100 preferences vs GAIL N=15 demos (noise axis, 11 levels × 30 seeds each).** New `scripts/plots/plot_noise_N100_N15.py` → `figures/noise_N100_N15_pt_vs_gail.png`. PT 218 → **−108** @100%; GAIL 265 → **−210**. Same shape as the bigger budgets — N=1000/N=100 (PT 270→−88, GAIL 264→−220) and N=350/N=50 (PT 262→−52, GAIL 263→−204): GAIL leads at low noise, **crosses below PT in the 40–60% band**, then collapses far lower while PT degrades gracefully. | The pattern is budget-independent, so **N=100/N=15 is a valid cheaper substitute for N=350/N=50**. The smallest pairing tried (PT N=50 / GAIL N=5) was **not** chosen — more randomness/variance on the PT side, less consistent. |
| 2026-07-09 | **Example-clip gallery for the supervisor (no datasets).** `figures/`: `pt_blank_b5_p{50,75}_20fps.mp4`, `pt_region{25,50,75}_20fps.mp4`, new `pt_region25_20fps_outline.mp4` (red border, so the mask is distinguishable from the black sky), `segment_pt_{CURRENT_0.40x_20fps,REALTIME_1.00x_50fps}.mp4` (speed axis), and new `pt_control_clean_20fps.mp4` / `pt_sticky_p50_20fps.mp4` / `pt_delay_k3_20fps.mp4`. New `scripts/preference/control_wrappers.py` (`make_control_corruptor`, a deliberate mirror of the one in `IRL3/imitation/human_demo.py` — keep in sync) + `scripts/preference/gen_control_example_clips.py` (rolls expert 4615187 under sticky/delay, renders 100 frames @ 20 fps; run with the **imitation-gail** python since it needs gymnasium+SB3). `mask_region_videos.py` gained `--outline COLOR/--outline_thickness` (drawn inside the box; occluded area unchanged — verified interior still black, ring RGB ≈ (254,0,0)). Measured on the clips: sticky p=0.5 changes only **19%** of the expert's actions (the expert holds actions, so repeating the previous one is usually a no-op) vs delay k=3 changing **42%**. | Supervisor wants to see each difficulty before we commit to a labelling round. **Honest caveat to present: for PT these are a data-distribution change, not a perceptual difficulty — the labeller still watches a perfect view at normal speed. Only GAIL feels sticky/delay as a real control difficulty.** |
| 2026-07-09 | **PT `--out_fps` option (implemented).** `scripts/preference/extract_segment_videos.py`: split the single `fps` variable into **`src_fps`** (read from the rollout HDF5 attr; used ONLY for `start_sec = frame_offset / src_fps`, i.e. which frames get cut) and **`out_fps`** (new `--out_fps`, default `None` = same as source = unchanged; adds `setpts=PTS*src/out` + `-r out` only when it differs). Warns above 60 fps (a display cannot show every frame → effectively frame-dropped). `metadata.pkl` now records both (`fps=out_fps`, `src_fps=src_fps`). `scripts/offline_data/build_rendered_mixture.py`: `f.attrs["fps"] = 20` **un-hardcoded** — `load_source()` now returns the source rollout's fps, all four variants must agree (hard error otherwise), and that value is written through. **Verified:** default extraction of pair 000 is **byte-identical** (`md5 7f8e64e2…`) to the clip actually labelled in `videos_human_n350_clean`; `--out_fps 50` yields 50 fps / **100 frames** / 2.000 s (same frame count ⇒ lossless re-time); both runs seek to the identical source frames (47…146), proving `out_fps` never leaks into the seek. Downstream `blank_segment_videos.py` / `mask_region_videos.py` pass no `-r`, so they inherit whatever rate the clip has. **Existing labelled clips were NOT re-timed** — `humanblock0`/`humanblock5p50` stay at 20 fps; re-rating them would confound blanking with speed, the same error found on the GAIL side. `--out_fps` is for future sets. | Make the PT clip rate an explicit choice without a re-render (frames are 1-per-step, so fps is pure playback), and stop the rollout fps and the mixture attr from silently disagreeing. |
| 2026-07-09 | **CONCEPT: PT has no throttle — the fps lives in the MP4 header.** Two clocks: **game time** = `steps × 1/50 s` (fixed; `FPS = 50` says how much game time one step *represents*, it does not make `env.step` take 20 ms — the sim has no clock); **wall time** = how long the labeller actually watches. **Real time ⇔ wall == game.** GAIL throttles a *live* loop with `time.sleep(1.0/fps)`; **PT has no live loop** — the clips are pre-rendered, so the number `20` is stamped into the MP4 at write time and the **browser's video player** does the waiting (one frame every 50 ms). Same job, different place. Consequence for a labelled clip: 100 frames = 100 steps = **2.0 s of game time**, played over **5.0 s** of wall time ⇒ **0.40× real time**. At 50 fps it would play in 2.0 s and the clocks would agree (= real time); 0.5 s would need 200 fps. Because frames are 1-per-step regardless, changing the playback rate needs **no re-render** — only a re-time (lossless up to 60 fps / 1.2× real time; beyond that frames must be dropped). GAIL's side of this (why `time.sleep` exists, why not `render_mode="human"`, the deadline bug) is in the IRL3 WORKLOG. | Recurring confusion about what "1×" means for a clip; and it decides whether the fps option needs re-rendering (it does not). |
| 2026-07-09 | **⚠️ SPEED AUDIT — the three arms run at three different speeds (confound). All figures PROVEN, see IRL3 WORKLOG for the evidence.** LunarLander's own default is **`FPS = 50`** (`lunar_lander.py:25` gym / `:36` gymnasium; `metadata["render_fps"] = 50`), i.e. 1 step = 20 ms ⇒ **real time ≡ 50 steps/s**. Measured `ep_length/duration_sec`: PT label clips **0.40×** real time (exact, by construction); GAIL clean `session_2` **0.45×** (requested `--fps 25`); GAIL `blank50` **0.76×** (requested `--fps 50`). **⇒ the b=10 GAIL human blanking result confounds blanking with a 2× requested / 1.7× achieved speed-up** — the blanked demos were played at ~real time, the clean baseline at half speed. Two comparison clips rendered: `figures/segment_pt_{CURRENT_0.40x_20fps,REALTIME_1.00x_50fps}.mp4` (same 100 frames, 5.000 s vs 2.000 s). **TODO (agreed, not yet done):** expose `--fps` when rendering PT segment MP4s, and expose a render/step-rate option for GAIL human demos; write a `session_meta.json` recording requested + achieved fps. | User asked to verify "GAIL=1×, PT=0.4×" before matching them. Both false: PT is 0.40×, clean GAIL 0.45×, blanked GAIL 0.76×. |
| 2026-07-09 | **PT segment MP4s: 20 fps, and the exact code path.** `scripts/offline_data/rollout_with_video.py` captures `env.render()` **once per `env.step`** (lines 116/137/141) and writes one MP4 per episode via `imageio.get_writer(fps=args.fps)` (lines 94-95), `--fps` **default 20** (line 47), stamping `f.attrs["fps"]` (line 170). → `scripts/offline_data/build_rendered_mixture.py:219` **hardcodes** `f.attrs["fps"] = 20` on the mixture HDF5. → `scripts/preference/extract_segment_videos.py` reads that attr and slices 100-frame clips (`slice_segment()`, ffmpeg re-encode). → `blank_segment_videos.py` / `mask_region_videos.py` paint over them → `label_web.py` serves them. (`render_lunarlander_segments.py` is a *separate* matplotlib renderer, **not** used for the human clips.) Proof: `episodes/index.pkl` ep 0 → `n_frames=175`, steps=175 ⇒ **1.00 frames/step**; `ffprobe` of a labeled clip ⇒ 600×400, 20 fps, 100 frames, 5.000 s. **So 100 frames = 100 steps = 2.0 s of lander time played over 5.0 s = 0.40× real time.** The fps is fixed in **two** places (`rollout_with_video.py --fps` default, and the hardcoded `build_rendered_mixture.py:219`); both must change together to re-render at another rate. | "Which file renders the segments, and at what fps?" — needed before making fps a user-chosen option. |
| 2026-07-09 | **Speed axis: measured, not assumed.** New `scripts/preference/gen_speed_examples.sh` → 10 example clips in `$SCRATCH/PT/lunarlander/speed_demo/` (isolated). **Key finding: the clips we label already play at 0.4× real time** — each rendered frame is one physics step (FPS=50, dt=20 ms), so 100 frames = 2.0 s of lander time shown over 5.0 s at 20 fps. Real time ≡ 50 fps. Lossless speed-up caps at **1.2× real time** (60 Hz display); past that you must **drop frames**, making fast-forward a frame-dropping perception difficulty, i.e. a cousin of blanking. Slow-down has **no** lower bound (verified 1 fps → 100 s clip). ⚠️ PT labels at 0.4× but GAIL plays at 1.0× — **the arms don't share a speed axis today**; pick one anchor first. Corrections to `DIFFICULTY_AXES.md`: PT gym is **0.23.1** (not 0.21); **gravity is EASY on both arms** (`env.world.gravity = (0,g)` is writable post-construction — kwarg still rejected), so gravity promoted; **wind is worse than thought** (`wind_idx` drawn once in `__init__` from the *global* NumPy RNG, never re-drawn in `reset()`, increments across episodes → `reset(seed=)` does not control wind and episodes are coupled), so wind demoted. | Supervisor gallery + honest feasibility limits before committing to a speed/gravity/wind study. |
| 2026-07-09 | **b=5 human plot now live (10 seeds).** Jobs 5774991 / 5775100 / 5775102-06 all COMPLETED 0:0. `figures/frame_blanking_human_b5_pt_vs_gail.png`: **PT 0% = +240, PT 50% = +215** (10 seeds each); GAIL 0% = +57 (10 seeds, demo return +237). Unlike b=10 (where 50% *beat* 0%: 277 vs 258), **b=5 shows the expected degradation** — the shorter 0.25 s blackouts hurt PT preferences where the 0.5 s ones did not. GAIL b=5 levels await human demos. | The b=10 null result was the reason for the b=5 redo; b=5 restores a monotone story on the PT side. |
| 2026-07-09 | **0% baseline topped up to 10 seeds (both arms).** PT: `sbatch --array=5-9 run_pt_pipeline_humanblock0.sh` → job **5775100** (script unchanged; array overridden on the CLI; `humanblock0-s5..s9` labels were the same-labels copies). GAIL: new `IRL3/scripts/submit_gail_blind_human_b5.sh <blank_pct> <seed_lo> <seed_hi>` (reuses `run_gail_lunarlander.sh`, N=50 / batch 512 / frame_skip 0 / 50-ep eval, matching `meta.json` of the b=10 human runs so seeds pool) → jobs **5775102-5775106** for blank0 seeds 5-9; appends to `experiments/gail_blind_human_b5_<date>.csv`. The b=5 plot merges old (s0-4) + new (s5-9) blank0 rows → 10 seeds. | User wants a concrete 10-seed b=5 plot; 0% is blank-agnostic so it just needs 5 more seeds, not new data. |
| 2026-07-09 | **B=5 human plot (train-as-you-label).** New `scripts/oracle_vs_human_vs_pebble/run_pt_pipeline_humanblock5p50.sh` (array 0-9 = **10 training seeds**, `--gres=gpu:l40s:1`, reward model + IQL on the `humanblock5p50` labels) → job **5774991**, outputs in `frame_blanking/humanblock5p50_mixture/seed_{0..9}`. New `scripts/plots/plot_frame_blanking_human_b5.py` **auto-discovers** levels (0/25/50/75): draws whatever has finished, skips the rest, so re-running it after each level lands adds the point. 0% is blank-agnostic → reuses PT `humanblock0_mixture` + GAIL `blind_human_blank0` (5 seeds each). **Workflow going forward: user labels a level → I immediately train it → replot.** | Wanted the b=5 curve, not b=10 (already plotted). GAIL b=5 levels await human demos. |
| 2026-07-09 | **PT human B=5 blanking, 50% level: labeled + converted.** Added opt-in `--resume` to `scripts/preference/label_web.py` (reloads an existing `--output` pickle into `LABELS`, verifying each entry's `pair_starts_a/b` against `index.pkl`; refuses on mismatch. Without it an existing output is overwritten on the first click — now warns). Recovered an interrupted session at 183/350 (first 183 verified byte-identical to backup), finished 350/350 (146 A / 152 B / 52 tie, 0 skips, 128.7 min) → `human_label/lunarlander-mixture-v2-humanblock5p50-s0..s9`. Extended `humanblock0` from `-s0..s4` to `-s0..s9` (same labels; seeds vary training only). | Terminal died mid-labeling; the labeller had no resume path and would have destroyed 66 min of work. B=5 redo needs 10 seeds. p75 still to label. |
| 2026-07-08 | **Region masking (spatial difficulty, Sweep 1) — PT human arm.** New `scripts/preference/mask_region_videos.py` (spatial twin of `blank_segment_videos.py`): paints ONE fixed black rectangle over **every** frame of a segment via ffmpeg drawbox; area = `region_frac`·frame (each side ×√frac → 0.25→300×200, 0.5→424×283, 0.75→520×346 on 600×400), location uniform-random **per segment**, seeded from `(region_seed,batch,pair,side)` so identical across training seeds; repoints `index.pkl` (same stale-path fix as blanking). `scripts/preference/gen_region_videos.sh` (sbatch) copies the CLEAN N=350 set → `region_mask/pt_human/videos_human_n350_region{25,50,75}` and masks in place. Verified: 50% box = 424×283, inside-box mean 0.00 on all 100 frames. Isolated in `region_mask/` (sibling of `frame_blanking/`); nothing existing modified. | Spatial occlusion axis (orthogonal to temporal blanking). Human-only this round; agent (blind-oracle) arm deferred. 0% reuses `humanblock0`. |
| 2026-07-03 | **Decision:** for noise injection keep **random (uniform) label/action replacement** — like ε-greedy — as the default, NOT exclude-expert / flip. Confirmed defaults are already uniform: `setup_grid_labels_ms.py --noise_mode=random_replace` (and `run_grid_ms_noise.sh NOISE_MODE=random_replace`). Flip (`deterministic_flip`) + exact-selection stay as opt-in ablation variants. | Uniform replacement leaves ~1/3 (PT) / ~1/4 (GAIL) accidentally-correct — the realistic "info-destruction" model; flip/exclude are the anti-signal ablation, not the default. Same policy for GAIL (see IRL3 WORKLOG). |
| 2026-07-02 | Frame-blanking reference facts: preference videos are **20 fps → each frame = 50 ms**; **`b` (`--block_len`) = blackout length in FRAMES**, so b=10 → 0.5 s, b=20 → 1 s (~50% of blocks blanked). Blind-oracle drops blanked-frame reward from the sum (= counts them as **0**, not minus/worst) — subtle count-bias, dominated by the terminal ±100 anyway; mean-normalize is the cleaner option if wanted. | Recurring "how long / what does b mean / what do blanked rewards become" questions. |
| 2026-07-02 | GAIL noise-injection variants: new `scripts/collect_expert_perframe_demos_online_noise_variants.py` (`--noise-mode` = coin_uniform/exact_uniform/coin_exclude/exact_exclude; original collector untouched) — `exclude` removes GAIL's ~1/4-correct floor (align 0.25→0.00 at 100%). New `scripts/plots/plot_noise_variants_pt_vs_gail.py` → `figures/noise_variants_pt_vs_gail.png` (2 panels random/flip, bold=exact + faint dashed=coin shadow). **Gotcha:** the original `plot_grid_pt_vs_gail.py` needs the `imitation-gail` env (has pyarrow) — the `pt` env errors on `import pyarrow`; run it as `/scratch/marzii/envs/imitation-gail/bin/python scripts/plots/plot_grid_pt_vs_gail.py`. Both plot scripts + output PNGs are separate; original is unchanged. | Symmetric noise story for Matt (PT flip vs GAIL exclude); keep the original noise plot reproducible without touching it. |
| 2026-07-01 | Documented (in `scripts/preference/README.md`) what a "seed" means for human runs: labels are identical across seeds (human labels once); seeds = reward-model+IQL **training seeds** (init/minibatch/eval) → measure training/RL variance for the band, not labeling variance. | Recurring confusion; verified `human100_mixture` s0–s4 share the same `label_human` hash. |
| 2026-07-01 | Frame-blanking corrections: PT human uses the REAL gym-frame pipeline (`extract_segment_videos.py`) on the **rendered** mixture `seed_0/render/mixture-v2` (not the matplotlib render / grid mixture); switched blanking to **contiguous blocks** (`block` mode, B=10) since single-frame blanks blend at 20 fps; new `blank_segment_videos.py` blanks real clips + repoints `index.pkl` (label_web followed stale absolute paths → served clean videos); `label_web` now sends `no-store`. Redid blind-oracle (#3) on the rendered mixture w/ matching B=10 blocks. | Get correct real-video human labeling + apples-to-apples human-vs-oracle on identical data. |
| 2026-07-01 | Launched blind-oracle PT training (reward+IQL on the **true 8-D mixture** + blind-oracle labels), 5 seeds × {0,25,50,75}%; outputs in `frame_blanking/`, `last10` eval. | Frame-blanking Exp 1 automated PT arm — agent solves the normal task, only labels degraded. |
| 2026-07-01 | Added opt-in frame-blanking as a *supervision* difficulty (Exp 1): blind-oracle labels (`setup_grid_labels_ms.py --blind_oracle_pcts`, scores visible-frame reward only) + human blanking in `render_lunarlander_segments.py --blank_mode`. Agent solves the normal 8-D task; outputs isolated in `frame_blanking/`; default-off. | Measure how "can't see" degrades *preference* quality (not the agent's task). Verified alignment 1.0→0.965→0.915→0.825 at 0/25/50/75% (flips only close comparisons). |
| 2026-06-30 | Re-ran all noise sweeps (random + flip × N=50/1000, 11 levels × 30 seeds) under `ex*` names. | Regenerate the noise axis under the exact-% convention without overwriting old results. |
| 2026-06-30 | Switched noise injection from per-label coin (`rng.random < p`) to exact-% selection (`rng.choice`); added `deterministic_flip` mode. | Coin made the corrupted *count* wobble at small N=50 (20% → ~14–26%); exact gives clean 20%. Flip = inverted labels (anti-signal) vs random = uniform replace (info destruction). |
| 2026-06-30 | Consolidated grid scripts into one gen→submit→run trio per axis; moved pipeline runs to `oracle_vs_human_vs_pebble/`, plots to `plots/`, smoke tests to `smoke_test/`. | Too many overlapping copies; now one clear pair per axis (noise/count). |
